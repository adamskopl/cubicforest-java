package org.adamsko.cubicforest.render.text;

/**
 * Enumerates all types of values that can be added (specific class fields like
 * 'name'). If class that implements LabelsMaster can't handle some values,
 * Exception should be raised.
 * 
 * @author adamsko
 * 
 */
public enum ROLabel_e {
	LABEL_TILEPOS, LABEL_HEIGHT, LABEL_NAME
}
